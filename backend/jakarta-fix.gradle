dependencies {
    implementation 'jakarta.persistence:jakarta.persistence-api:3.1.0'
}

// Fix for javax.persistence to jakarta.persistence migration
configurations.all {
    resolutionStrategy.dependencySubstitution {
        substitute module('javax.persistence:javax.persistence-api') using module('jakarta.persistence:jakarta.persistence-api:3.1.0')
    }
}
